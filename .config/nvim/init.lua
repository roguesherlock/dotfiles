-- bootstrap lazy.nvim, LazyVim and your plugins
-- TODO:
--  X. add colorscheme plugin to auto update other apps' themes -- it only took me like 10 hours
--  X. figure out why is the sign column not working properly for modus theme
--  X. add proper modus themes for other apps
--  X. update the lualine. Don't need that breadcrumbs thing
--  X. figure out why is saving twice
--  6. why does the homepage show the signcolumn?
--  X. why does neovim resize the terminal?
--  8. Ability to ask ai for errors from typescript
--  X. Ability to search all files in the cwd (projec)
--  X. Figure out why the fuck does lazyvim override my keymaps
-- 11. Set import preferences for typescript lsp
--  X. Don't use ai completions with blink.nvim
-- 13. blink.nvim shouldn't insert when moving between options with ctrl-n/p

require("config.lazy")
