-- TODO:
-- [] Toggle window maximize with wm rather than just maximizing once
-- ~[] Figure out a way to show virtual lines only in normal mode~
-- [] Add a plugin to automatically fold imports on buffread and after imports
-- [] set buffer type on demand
-- [] format selection or buffer or current line on demand

require("config.options")
require("config.keymaps")
require("core.lazy")
require("core.lsp")
require("config.autocmds")
